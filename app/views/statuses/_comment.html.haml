%p
  - if source.proposition.present?
    [ 주장 #{link_to source.proposition.title, source.proposition} ]
  = auto_link source.contents
%p
  %small.text-muted
    %i.glyphicon.glyphicon-user
    %small= source.user.email
    %i.glyphicon.glyphicon-time
    %small= timeago_tag source.created_at
